import * as THREE from "three";

export function addSpectaclesAndHair(scene: THREE.Scene, character: THREE.Object3D) {
  // Cleaned 3D scene meshes for 2D interactive overlay setup
}

export function updateCustomAccessories(scene: THREE.Scene, headBone: THREE.Object3D) {
  // No-op
}
